# Physics Lab 3D 開發記憶

## 已知決策

專案採用 WebDev 的 `web-static` 腳手架，遊戲引擎採 Babylon.js。畫面主題是「天窗物理劇場」：暖白研究室背景、石墨文字、天窗鈷藍觀察線與琥珀能量提示。大型視覺資產不放進專案樹，必須使用 WebDev 靜態資產 URL。

## 初始化備註

第一次初始化時遇到暫時性的 artifacts Git remote DNS 解析錯誤，但本地專案已建立並可繼續編輯。完成一個小幅變更後，再透過 checkpoint 重試同步。

## 驗證紀錄

尚未開始視覺驗證。完成第一版後，以 `?demo` 進入 deterministic demonstration，再檢查 Babylon canvas、雙語面板、量測數值、軌跡與窄螢幕佈局。

